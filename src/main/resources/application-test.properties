# H2 for tests
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driver-class-name=org.h2.Driver
spring.jpa.hibernate.ddl-auto=create-drop

# Pas de seed de démo pendant les tests (contexte plus prévisible)
app.seed.demo-movies.enabled=false
app.seed.demo-reviews.enabled=false
